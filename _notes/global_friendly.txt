	$player.maxhealth = $player.health (global/friendly.scr, 1899)
	$player.maxhealth = $player^

^~^~^ Script Error: Field 'health' applied to NULL listener

	$player.maxhealth = $player.health (global/friendly.scr, 1899)
	$player^

^~^~^ Script Error: Field 'maxhealth' applied to NULL listener

Spawned friendly 4
Spawned friendly 3
Spawned friendly 2
Spawned friendly 1

			if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination^

^~^~^ Script Error: Field 'origin' applied to NULL listener

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination.origin ^

^~^~^ Script Error: binary '-' applied to incompatible types 'NIL' and 'vector'

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (^

^~^~^ Script Error: Cannot cast 'NIL' to vector

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination.origin - self.origin) ^

^~^~^ Script Error: binary '>' applied to incompatible types 'NIL' and 'int'


				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination.origin - self.origin) ^

^~^~^ Script Error: binary '>' applied to incompatible types 'NIL' and 'int'

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination^

^~^~^ Script Error: Cannot cast 'array' to listener

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination.origin ^

^~^~^ Script Error: binary '-' applied to incompatible types 'NIL' and 'vector'

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (^

^~^~^ Script Error: Cannot cast 'NIL' to vector

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination.origin - self.origin) ^

^~^~^ Script Error: binary '>' applied to incompatible types 'NIL' and 'int'

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination^

^~^~^ Script Error: Cannot cast 'array' to listener

				if (vector_length (self.destination.origin - self.origin) > self.distance + local.friendrange)  (global/friendly.scr, 391)
				if (vector_length (self.destination.origin ^

^~^~^ Script Error: binary '-' applied to incompatible types 'NIL' and 'vector'