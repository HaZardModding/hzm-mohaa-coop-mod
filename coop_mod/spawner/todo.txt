[] Implement spawning enemies in amount depending on difficulty level.
    - [] spawning should happen when player cannot see spawner.
    - [] spawning should happen in waves, possibly spawning enemy as nonsolid for short duration
         to parnoidally prevent from bots being stuck in each other.

[] Store enemy models depending on level in array, in separate file, to not break level immersion.
    - [] implement random choosing of enemy model.

[] Implement detection of alarmed state of enemies and if alarms were triggered, if yes, spawn bots
    and make them aggresively chase players.

[] Spawnpoints should be set in separate file.
    - [] spawn locations should change with players advancement through the map.